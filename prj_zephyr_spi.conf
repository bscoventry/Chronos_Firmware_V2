# Zephyr SPI test: use Zephyr SPI driver on spi20 instead of nrfx.
# Build with this as base conf (and zephyr_spi overlay) so CONFIG_SPI=y is set.
# Code uses #if defined(CONFIG_SPI) to run Zephyr SPI path; otherwise nrfx path.

CONFIG_SPI=y
